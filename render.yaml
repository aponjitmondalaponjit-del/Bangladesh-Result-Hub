services:
  - type: web
    name: bangladesh-result-hub
    runtime: python
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.10
      - key: BOT_TOKEN
        sync: false
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
